{
  "name": "aws-dynamodb",
  "version": "1.0.0",
  "description": "aws and DynamoDB with node and aws-sdk",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "Dynamodb",
    "node",
    "express"
  ],
  "author": "Raymond Greeley",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.316.0",
    "@aws-sdk/util-dynamodb": "^3.316.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
